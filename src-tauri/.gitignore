# Generated by Cargo / Tauri
/target/
/gen/schemas/

# Nuitka backend sidecar build output (built via `make backend-binary`).
# Keep the folder + this README so the Tauri resource glob always resolves.
binaries/ops-intel-agent-backend/*
!binaries/ops-intel-agent-backend/README.md
