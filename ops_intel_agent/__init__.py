"""Intelligent IT Operations Agent.

RAG + LLM system that turns raw server-error logs into:
  * a plain-language explanation for end users,
  * a concrete triage playbook for on-call engineers,
and deposits resolved incidents back into a vector knowledge base so the
system gets smarter over time.
"""

__version__ = "0.1.0"
